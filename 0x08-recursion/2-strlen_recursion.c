#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * int _strlen_recursion - returns the length of a string.
 * @s: string
 *
 * Return: length
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
