#include "main.h"
#include <stdio.h>
#include <unistd.h>
#include <string.h>
/**
 * _puts- function that prints a string, followed by a new line, to stdout.
 * @str: a string
 */
void _puts(char *str)
{
	puts(str);
}
